# Status and ToDo

## Status
Working with USE_AWS=TRUE, without docker, with docker and with k8s.
Not working with USE_AWS=FALSE.
TESTS failing in both cases.

## ToDo:
1. Kubernetes training
2. Next thing in AWS
3. integrate multipr / -tr / asyncio from playground
4. Keep a ref to the files ina  db / list and delete any uploaded files / select a file for conversation
5. Celery?
6. user stories use jira
7. pylint / black -> run on other pc -> review